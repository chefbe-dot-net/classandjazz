<nav id="menu">
  <ul>
    *{menu[lang] as entry}{
      <li ?{entry["menu"]}{class="dir"}>
        <a href="@{${entry.url}}">${entry.label}</a>
        ?{entry["menu"]}{
          <ul>
          *{entry["menu"] as entry}{
            <li>
              <a href="@{${entry.url}}">${entry.label}</a>
            </li>
          }
          </ul>
        }
      </li>
    }
  </ul>
</nav>

